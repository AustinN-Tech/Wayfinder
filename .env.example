# Copy this file to .env and fill in real values. .env is gitignored - never commit real secrets.

GEMINI_API_KEY=
# Optional - only needed if you want to lock down CORS instead of allowing all origins
FRONTEND_ORIGIN=

# Auth0 - both are public identifiers, not secrets, but still keep them out of git
AUTH0_DOMAIN=
AUTH0_AUDIENCE=
